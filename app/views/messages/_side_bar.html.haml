.side-bar
  .side-bar__header
    .side-bar__header__box
      %p.side-bar__header__box__name
        = current_user.name
      %ul.side-bar__header__box__option
        %li.side-bar__header__box__option__new-group 
          = link_to new_group_path(current_user) do
            = icon('far' , 'edit' , class: 'icon')      #{}と()使って書く方法二通り?
        %li.side-bar__header__box__option__edit-user
          = link_to edit_user_path(current_user) do
            = icon('fas' , 'cog' , class: 'icon')       #{}と()使って書く方法二通り?
  .side-bar__group-list 
    - current_user.groups.each do |group|
      .side-bar__group-list__group
        = link_to '#' do
          %p.side-bar__group-list__group__name
            = group.name
          %p.side-bar__group-list__group__message
            まだメッセージがありません


// MEMO //

// 部分テンプレートであるサイドバーファイル！ //
// よくあるミス!! link_to "edit_user_path" do ではなく edit_user_path   **文字列ではないので注意** //
// 11行目 ===>  current_userでOK!! current_user.idでは？と思っていたのは、過去のカリキュラムのcreateアクションでmerge(user_id: current_user.id)としてDBにidを保存したかったから！
// デフォルトではHTTPメソッドはGET! //